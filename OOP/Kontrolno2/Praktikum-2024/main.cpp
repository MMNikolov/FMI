#include "Artifacts.hpp"

int main()
{
    srand(time(NULL));
    
    try
    {
        
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }
    
    return 0;
}